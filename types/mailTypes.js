const Mailchimp = require("@mailchimp/mailchimp_transactional");

/**
 * @typedef { { type: string, name: string, content: string}[] } MailAttachment - an array of object containing attachments data
 */
